import 'package:flutter/material.dart';

class ComunicacionUnivScreen extends StatelessWidget {
  const ComunicacionUnivScreen({super.key});
  static const String routeName = "/ComunicacionUniversitaria";

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Comunicacion Universitaria')),
    );
  }
}
