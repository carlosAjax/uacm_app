import 'package:flutter/material.dart';

class PublicoGeneralScreen extends StatelessWidget {
  const PublicoGeneralScreen({super.key});
  static const String routeName = "/PublicoGeneralScreen";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pubico en General')),
      body: Container(),
    );
  }
}
